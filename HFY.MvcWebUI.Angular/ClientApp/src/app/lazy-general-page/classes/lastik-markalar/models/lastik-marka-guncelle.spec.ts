import { LastikMarkaGuncelle } from './lastik-marka-guncelle';

describe('LastikMarkaGuncelle', () => {
  it('should create an instance', () => {
    expect(new LastikMarkaGuncelle()).toBeTruthy();
  });
});
