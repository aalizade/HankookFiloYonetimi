import { AksPozisyonGuncelle } from './aks-pozisyon-guncelle';

describe('AksPozisyonGuncelle', () => {
  it('should create an instance', () => {
    expect(new AksPozisyonGuncelle()).toBeTruthy();
  });
});
