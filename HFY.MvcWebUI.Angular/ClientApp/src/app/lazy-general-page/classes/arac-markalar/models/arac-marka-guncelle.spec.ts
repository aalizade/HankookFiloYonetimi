import { AracMarkaGuncelle } from './arac-marka-guncelle';

describe('AracMarkaGuncelle', () => {
  it('should create an instance', () => {
    expect(new AracMarkaGuncelle()).toBeTruthy();
  });
});
