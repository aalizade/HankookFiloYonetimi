import { HizIndeksGuncelle } from './hiz-indeks-guncelle';

describe('HizIndeksGuncelle', () => {
  it('should create an instance', () => {
    expect(new HizIndeksGuncelle()).toBeTruthy();
  });
});
