import { YukIndeksGuncelle } from './yuk-indeks-guncelle';

describe('YukIndeksGuncelle', () => {
  it('should create an instance', () => {
    expect(new YukIndeksGuncelle()).toBeTruthy();
  });
});
